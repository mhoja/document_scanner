name: tra_smart_scanner
description: Tanzania Revenue Authority Smart Document Scanner
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.2
  camera: ^0.10.5
  path_provider: ^2.1.1
  image_picker: ^1.0.4
  cunning_document_scanner: ^1.2.3
  fluttertoast: ^8.2.2
  dio: ^5.4.0
  url_launcher: ^6.1.11
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/